import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'misComponetes2023DAMA11',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
